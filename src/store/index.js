import Vuex from 'vuex'
import Vue from 'vue'

Vue.use(Vuex)

const state = {}
const getters = {}
const mutations = {}

const store = new Vuex.Store({
  state,
  getters,
  mutations
})

export default store
